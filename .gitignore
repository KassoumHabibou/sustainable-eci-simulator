# large raw data (> GitHub limits) — regenerate/copy locally, see README
data/bilateral_2022.csv

# caches & local artifacts (keep state.pkl — required by the deployed app)
results/cache/*
!results/cache/state.pkl
__pycache__/
*.pyc
.DS_Store
.venv/
.streamlit/secrets.toml
